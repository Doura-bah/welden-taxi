/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package welden.taxi.app;

import welden.taxi.list.LinkedList;

import static welden.taxi.utilities.StringUtils.join;
import static welden.taxi.utilities.StringUtils.split;
import static welden.taxi.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
